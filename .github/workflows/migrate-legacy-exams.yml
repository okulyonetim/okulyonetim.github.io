name: migrate-legacy-exams

on:
  push:
    branches: [main]
    paths:
      - 'scripts/migrate-legacy-exams.py'

permissions:
  contents: write

jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
      - name: Apply legacy exams migration
        run: python3 scripts/migrate-legacy-exams.py
      - name: Syntax checks
        run: node --check js/modules/dashboard.js
      - name: Dashboard checks
        run: |
          node tests/dashboard-card-routes-smoke.test.js
          node tests/dashboard-reminders-smoke.test.js
      - name: Architecture checks
        run: |
          node tests/module-bundles-smoke.test.js
          node tests/modules-local-first-boundary.test.js
          node scripts/check-client-architecture.mjs
      - name: Cleanup temporary migration files
        run: |
          rm -f scripts/migrate-legacy-exams.py
          rm -f .github/workflows/migrate-legacy-exams.yml
      - name: Commit production migration
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          if git diff --cached --quiet; then exit 0; fi
          git commit -m 'feat(dashboard): restore legacy exams card layout'
          git push origin main
