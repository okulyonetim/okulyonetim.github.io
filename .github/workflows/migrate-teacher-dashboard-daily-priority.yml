name: Migrate Teacher Dashboard Daily Priority

on:
  push:
    branches: [main]
    paths:
      - 'scripts/migrate-teacher-dashboard-daily-priority.cjs'
      - '.github/workflows/migrate-teacher-dashboard-daily-priority.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Apply teacher dashboard migration
        run: node scripts/migrate-teacher-dashboard-daily-priority.cjs
      - name: Verify dashboard behavior
        run: |
          node --check js/modules/dashboard.js
          node --check js/core/shell-ui.js
          node tests/dashboard-card-routes-smoke.test.js
          node tests/dashboard-reminders-smoke.test.js
          node scripts/check-client-architecture.mjs
      - name: Build client bundles
        run: node scripts/build-client-bundles.mjs
      - name: Commit production package and cleanup
        run: |
          rm -f scripts/migrate-teacher-dashboard-daily-priority.cjs
          rm -f .github/workflows/migrate-teacher-dashboard-daily-priority.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add js/modules/dashboard.js js/core/shell-ui.js css/design-system.css tests/dashboard-card-routes-smoke.test.js scripts/migrate-teacher-dashboard-daily-priority.cjs .github/workflows/migrate-teacher-dashboard-daily-priority.yml
          git commit -m "feat(dashboard): prioritize teacher daily workflow"
          git push
