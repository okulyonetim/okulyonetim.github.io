name: Temporary Puantaj Diploma Polish

on:
  push:
    branches:
      - "feat/puantaj-report-polish"

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify temporary workflow
        run: echo "temporary workflow is running"
