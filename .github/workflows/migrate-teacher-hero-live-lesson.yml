name: Migrate Teacher Hero Live Lesson

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/migrate-teacher-hero-live-lesson.yml'
      - 'scripts/migrate-teacher-hero-live-lesson.py'

permissions:
  contents: write

jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Apply live hero lesson migration
        run: python3 scripts/migrate-teacher-hero-live-lesson.py
      - name: Verify dashboard behavior
        run: |
          node --check js/modules/dashboard.js
          node tests/dashboard-card-routes-smoke.test.js
          node tests/dashboard-reminders-smoke.test.js
          node scripts/check-client-architecture.mjs
      - name: Build client bundles
        run: node scripts/build-client-bundles.mjs
      - name: Commit production package and cleanup
        run: |
          rm -f scripts/migrate-teacher-hero-live-lesson.py .github/workflows/migrate-teacher-hero-live-lesson.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add js/modules/dashboard.js tests/dashboard-card-routes-smoke.test.js scripts/migrate-teacher-hero-live-lesson.py .github/workflows/migrate-teacher-hero-live-lesson.yml
          git commit -m 'feat(dashboard): sync teacher hero with live lesson status'
          git push
