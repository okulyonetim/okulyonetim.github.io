name: Migrate Weekly Duty Self Highlight

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/migrate-week-duty-self-highlight.yml'
      - 'scripts/migrate-week-duty-self-highlight.py'

permissions:
  contents: write

jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Apply weekly duty self highlight
        run: python3 scripts/migrate-week-duty-self-highlight.py
      - name: Verify dashboard behavior
        run: |
          node --check js/modules/dashboard.js
          node tests/dashboard-card-routes-smoke.test.js
          node tests/dashboard-reminders-smoke.test.js
          node scripts/check-client-architecture.mjs
      - name: Build client bundles
        run: node scripts/build-client-bundles.mjs
      - name: Commit production package and cleanup
        run: |
          rm -f scripts/migrate-week-duty-self-highlight.py .github/workflows/migrate-week-duty-self-highlight.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add js/modules/dashboard.js css/design-system.css tests/dashboard-card-routes-smoke.test.js scripts/migrate-week-duty-self-highlight.py .github/workflows/migrate-week-duty-self-highlight.yml
          git commit -m 'feat(dashboard): highlight teacher weekly duty'
          git push
