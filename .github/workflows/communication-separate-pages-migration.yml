name: Communication Separate Pages Migration

on:
  push:
    branches: [tmp-communication-pages-migration]
    paths:
      - 'scripts/apply-communication-separate-pages.mjs'
      - 'tests/communication-separate-pages.test.js'
      - '.github/workflows/communication-separate-pages-migration.yml'

permissions:
  contents: write

jobs:
  migrate:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
        with:
          ref: tmp-communication-pages-migration
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
      - name: Apply guarded Communication separate-page migration
        run: node scripts/apply-communication-separate-pages.mjs
      - name: Verify Communication separate pages
        run: node tests/communication-separate-pages.test.js
      - name: Verify communication local-first contract
        run: node tests/communication-local-first-contract.test.js
      - name: Verify bundles
        run: node scripts/build-client-bundles.mjs && node tests/module-bundles-smoke.test.js
      - name: Verify architecture
        run: node scripts/check-client-architecture.mjs
      - name: Commit production migration
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add js/modules/communication.js js/core/shell-ui.js
          git diff --cached --quiet && exit 0
          git commit -m 'refactor(communication): open communication screens as separate pages'
          git push origin HEAD:tmp-communication-pages-migration
