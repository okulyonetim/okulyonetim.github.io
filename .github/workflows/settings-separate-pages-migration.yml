name: Settings Separate Pages Migration

on:
  push:
    branches: [tmp-settings-pages-migration]
    paths:
      - 'scripts/apply-settings-separate-pages.mjs'
      - 'tests/settings-separate-pages.test.js'
      - '.github/workflows/settings-separate-pages-migration.yml'

permissions:
  contents: write

jobs:
  migrate:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
        with:
          ref: tmp-settings-pages-migration
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
      - name: Apply guarded Settings migration
        run: node scripts/apply-settings-separate-pages.mjs
      - name: Verify Settings separate pages
        run: node tests/settings-separate-pages.test.js
      - name: Verify bundles
        run: node scripts/build-client-bundles.mjs && node tests/module-bundles-smoke.test.js
      - name: Verify architecture
        run: node scripts/check-client-architecture.mjs
      - name: Commit production migration
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add js/modules/settings.js js/core/shell-ui.js
          git diff --cached --quiet && exit 0
          git commit -m 'refactor(settings): replace tabs with landing and direct pages'
          git push origin HEAD:tmp-settings-pages-migration
