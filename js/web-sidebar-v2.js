/* Koruk Asistan v2 — eski yalnız-web sidebar runtime'ı devre dışı.
   Navigasyon davranışı tek responsive uygulama kabuğuna taşınacaktır. */
(function(){ 'use strict'; window.__webSidebarV2=false; })();
