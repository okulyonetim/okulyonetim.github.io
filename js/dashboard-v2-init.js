/* Koruk Asistan v2 — eski yalnız-masaüstü dashboard/web shell başlangıcı devre dışı.
   Tek responsive uygulama kabuğu kurulurken bu uyumluluk dosyası tamamen kaldırılacaktır. */
(function(){ 'use strict'; window.__KORUK_WEB_SHELL_INIT__=false; })();
